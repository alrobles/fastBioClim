% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bio_11.R
\name{bio_11}
\alias{bio_11}
\title{bio_11 — Mean Temperature of Coldest Quarter (BIO11)}
\usage{
bio_11(tas, wrap = FALSE, filename = "", na_rm = FALSE)
}
\arguments{
\item{tas}{A \link[terra:SpatRaster-class]{terra::SpatRaster} with \strong{12 layers} of monthly mean
temperature (Jan–Dec).}

\item{wrap}{Logical; if \code{TRUE}, allow wrap‑around rolling quarters
(e.g., Dec–Jan–Feb). If \code{FALSE} (default), only non‑wrapping windows
\code{[1..3], [2..4], ..., [10..12]} are considered.}

\item{filename}{Optional file path for writing the output raster
(use \code{""} for in‑memory).}

\item{na_rm}{Logical; if \code{TRUE}, ignore \code{NA}s when selecting and
averaging quarters. If \code{FALSE} (default), any \code{NA} in a window
propagates to the result.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} with one layer representing \strong{BIO11}
(Mean Temperature of Coldest Quarter, units °C).
}
\description{
Compute \strong{BIO11 (Mean Temperature of Coldest Quarter)} from monthly mean
temperature (\code{tas}) rasters following the CHELSA/WorldClim Bioclimatic
Variables framework.
}
\details{
\strong{BIO11} is the \strong{average monthly mean temperature over the coldest
3‑month period} of the year, computed per cell. This implementation:
\enumerate{
\item Identifies, for each cell, the starting month of the \strong{coldest
quarter} by scanning rolling 3‑month windows over \code{tas}
(optionally allowing wrap‑around windows, e.g., Dec–Jan–Feb when
\code{wrap = TRUE}) and selecting the window with the \strong{minimum
score} (see NA handling below).
\item Uses that quarter index to compute the \strong{mean of the three monthly
mean temperatures} from \code{tas} for the same months.
}

The rolling quarter index is obtained with
\code{parallel_which_min_quarter(tas_matrix, wrap = ..., na_rm = ...)}.
The quarter mean temperature is then computed with
\code{parallel_average_quarter(idx, mat, wrap = ..., na_rm = ...)}.
}
\section{NA handling}{

\itemize{
\item If \code{na_rm = TRUE}, missing values (\code{NA}) within a 3‑month window
are ignored when computing window scores (for selection) and the final
quarter mean. A window with \strong{all \code{NA}} values yields \code{NA};
if \strong{no valid windows} exist for a row/cell, the result is \code{NA}.
\item If \code{na_rm = FALSE} (default), any \code{NA} in a 3‑month window
\strong{invalidates} that window for selection; the final quarter mean will also
become \code{NA} if the selected window contains any \code{NA}.
}
}

\section{Inputs and units}{

\itemize{
\item \code{tas}: monthly mean temperature (°C), 12 layers (Jan–Dec).
}

The raster must be a \code{terra::SpatRaster} with 12 layers. When writing to
disk, the output inherits geometry (extent, resolution, CRS) from \code{tas}.
}

\examples{
# Mock data (12-layer monthly rasters)
tas_ex <- mock_tas() # mean temperature (°C)

# Compute BIO11 without wrap-around (only Jan–Dec contiguous quarters)
bio11 <- bio_11(tas_ex, wrap = FALSE)
bio11

# Allow wrap-around quarters (e.g., Dec–Jan–Feb)
bio11_wrap <- bio_11(tas_ex, wrap = TRUE)
bio11_wrap

# Handle NA values by ignoring them during selection and averaging
tas_na <- make_1x1_12(c(NA, NA, -5, -3, -2, -1, 0, 1, 2, 3, 4, 5))
bio11_na <- bio_11(tas_na, wrap = FALSE, na_rm = TRUE)
bio11_na

}
\references{
Karger, D.N., et al. (2022). CHELSA V2.1: High‑resolution monthly and annual
climatologies for the Earth land surface areas.
\emph{Earth System Science Data}, 14, 5573–5610. \cr
Hijmans, R.J., et al. (2005). Very high resolution interpolated climate
surfaces for global land areas. \emph{International Journal of Climatology},
25(15), 1965–1978. \cr
CHELSA Bioclim dataset overview:
\url{https://www.chelsa-climate.org/datasets/chelsa_bioclim}
}
\seealso{
\code{\link[=parallel_which_min_quarter]{parallel_which_min_quarter()}}, \code{\link[=parallel_average_quarter]{parallel_average_quarter()}}
}
