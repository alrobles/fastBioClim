% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_row_max.R
\name{parallel_row_max}
\alias{parallel_row_max}
\title{Row-wise maximum (parallel)}
\usage{
parallel_row_max(mat)
}
\arguments{
\item{mat}{A \strong{numeric matrix} with rows representing spatial units (e.g.,
grid cells) and columns representing measurements (e.g., the 12 months).}
}
\value{
A \strong{numeric vector} of length \code{nrow(mat)} with the row-wise maximum
values. Row names are preserved (if present).
}
\description{
Fast row-wise \strong{maximum value} using a parallel C++ implementation via
\pkg{RcppParallel}. This is tailored for large matrices derived from rasters
(rows = grid cells, columns = months), which is a common layout when working
with CHELSA monthly climatologies.
}
\details{
Typical CHELSA Bioclim use cases include computing:
\itemize{
\item \strong{BIO5} (Max Temperature of Warmest Month): the per-cell maximum across
the 12 monthly temperature layers of the relevant CHELSA temperature
product.
\item Analogous operations for other variables where the month-wise maximum is
an intermediate step.
}

References:
\itemize{
\item CHELSA Bioclim dataset:
\url{https://www.chelsa-climate.org/datasets/chelsa_bioclim}
\item Karger et al. (2022). CHELSA V2.1: High-resolution monthly and annual
climatologies for the Earth land surface areas.
\emph{Earth System Science Data}, 14, 5573–5610.
}
}
\section{NA handling}{

If any \code{NA}/\code{NaN} is present in a row, the result for that row is \code{NA}
(equivalent to \code{na.rm = FALSE}). Rows with zero columns are invalid and
should be filtered upstream.
}

\examples{
# Simple example: rows are cells, columns are months
mat <- matrix(1:9,
  ncol = 3,
  dimnames = list(paste0("cell", 1:3), paste0("m", 1:3))
)
# Rows are: c(1,4,7), c(2,5,8), c(3,6,9) -> maxima: 7, 8, 9
mx <- parallel_row_max(mat)
mx

# CHELSA-style usage:
tas <- mock_tas()
# (cells x 12 months), you could get the warmest month value per cell as:
tas_matrix <- terra::as.matrix(tas)
warmest <- parallel_row_max(tas_matrix)
all(warmest == 12)
}
\seealso{
\code{\link[=parallel_row_min]{parallel_row_min()}}, \code{\link[=parallel_sd]{parallel_sd()}}, \code{\link[=parallel_variance]{parallel_variance()}}
}
