% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_row_min.R
\name{parallel_row_min}
\alias{parallel_row_min}
\title{Row-wise minimum (parallel)}
\usage{
parallel_row_min(mat)
}
\arguments{
\item{mat}{A \strong{numeric matrix} with rows representing spatial units (e.g.,
grid cells) and columns representing measurements (e.g., the 12 months).}
}
\value{
A \strong{numeric vector} of length \code{nrow(mat)} with the row-wise minimum
values. Row names are preserved (if present).
}
\description{
Fast row-wise \strong{minimum value} using a parallel C++ implementation via
\pkg{RcppParallel}. Designed for large, tall matrices created from raster
stacks (rows = cells, columns = months), as commonly used with CHELSA
climatologies.
}
\details{
Typical CHELSA Bioclim use cases include computing:
\itemize{
\item \strong{BIO6} (Min Temperature of Coldest Month): the per-cell minimum across
the 12 monthly temperature layers of the relevant CHELSA temperature
product.
\item Analogous operations for other variables where the month-wise minimum is
an intermediate step.
}

References:
\itemize{
\item CHELSA Bioclim dataset:
\url{https://www.chelsa-climate.org/datasets/chelsa_bioclim}
\item Karger et al. (2022). CHELSA V2.1: High-resolution monthly and annual
climatologies for the Earth land surface areas.
\emph{Earth System Science Data}, 14, 5573–5610.
}
}
\section{NA handling}{

If any \code{NA}/\code{NaN} is present in a row, the result for that row is \code{NA}
(equivalent to \code{na.rm = FALSE}). Rows with zero columns are invalid and
should be filtered upstream.
}

\examples{
mat <- matrix(1:9,
  ncol = 3,
  dimnames = list(paste0("cell", 1:3), paste0("m", 1:3))
)
mn <- parallel_row_min(mat)
mn

# CHELSA-style usage:
tas <- mock_tas()
# Suppose 'tas' values were read blockwise to a matrix 'v'
# (cells x 12 months), you could get the warmest month value per cell as:
tas_matrix <- terra::as.matrix(tas)
coldest <- parallel_row_max(tas_matrix)
all(coldest == 12)
}
\seealso{
\code{\link[=parallel_row_max]{parallel_row_max()}}, \code{\link[=parallel_sd]{parallel_sd()}}, \code{\link[=parallel_variance]{parallel_variance()}}
}
