# Load terra (modern raster package)
library(terra)
tas_fake <- mock_tas()

tasmax_fake <- mock_tasmax()
tasmin_fake <- mock_tasmin()
tas_fake == (tasmax_fake + tasmin_fake)/2 

r <-mock_tasmax()
as.matrix(r)
